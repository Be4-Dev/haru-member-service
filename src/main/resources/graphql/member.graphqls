type Query {
    member(id: ID): Member
}

type Mutation {
    registerMember(command: RegisterMemberCommand): Member
}

input RegisterMemberCommand {
    nickname    : String
    email       : String
    password    : String
    createdBy   : String
}